# Use Open AI with key
provider: autogen_ext.models.openai.OpenAIChatCompletionClient
config:
  model: gpt-4o
  api_key: REPLACE_WITH_YOUR_API_KEY
# If you use a custom/unknown model alias, provide model_info (example below):
#   model_info:
#     vision: true
#     function_calling: true
#     json_output: true
#     structured_output: true
#     family: gpt-4o
# Use Azure Open AI with key
# provider: autogen_ext.models.openai.AzureOpenAIChatCompletionClient
# config:
#   model: gpt-4o
#   azure_endpoint: https://{your-custom-endpoint}.openai.azure.com/
#   azure_deployment: {your-azure-deployment}
#   api_version: {your-api-version}
#   api_key: REPLACE_WITH_YOUR_API_KEY
# Note: For Azure, keep 'model' as a valid base model like 'gpt-4o' and put your
#       custom deployment name in 'azure_deployment'. If you must use a custom
#       model name in 'model', also add a matching 'model_info' as shown above.
# Use Azure OpenAI with AD token provider.
# provider: autogen_ext.models.openai.AzureOpenAIChatCompletionClient
# config:
#   model: gpt-4o
#   azure_endpoint: https://{your-custom-endpoint}.openai.azure.com/
#   azure_deployment: {your-azure-deployment}
#   api_version: {your-api-version}
#   azure_ad_token_provider:
#     provider: autogen_ext.auth.azure.AzureTokenProvider
#     config:
#       provider_kind: DefaultAzureCredential
#       scopes:
#         - https://cognitiveservices.azure.com/.default
